groups:
  - name: blackbox_icmp_home
    rules:
      - alert: РоутерНеВідповідає
        expr: "probe_success{job='blackbox_icmp_home', env='telegram_home'} == 0"
        for: 5m
        labels:
          severity: error
        annotations:
          summary: "Немає зв`язку з домашнім роутером більш ніж 5 хвилин"
